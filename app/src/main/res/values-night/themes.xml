<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.GrammarHelper" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Dark Theme - Comfortable for reading -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Dark background colors - Soft dark, not pure black -->
        <item name="android:colorBackground">@color/dark_background</item>
        <item name="android:windowBackground">@color/dark_background</item>

        <!-- Card and surface colors -->
        <item name="colorSurface">@color/dark_surface</item>
        <item name="colorOnSurface">@color/dark_primary_text</item>

        <!-- Text colors - High contrast for readability -->
        <item name="android:textColorPrimary">@color/dark_primary_text</item>
        <item name="android:textColorSecondary">@color/dark_secondary_text</item>
        <item name="android:textColorHint">@color/dark_hint</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/dark_background</item>

        <!-- Navigation bar -->
        <item name="android:navigationBarColor">@color/dark_background</item>

        <!-- Toolbar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>